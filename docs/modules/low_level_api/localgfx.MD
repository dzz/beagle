#localgfx
```python
from localgfx import *


img_create( int w, int h )      # returns handle to blank image in system memory
img_load( string file )         # returns handle to loaded image in system memory
img_drop( uint handle )         # deletes an image from system meory
img_dims( uint handle )         # returns tuple of width and height of system memory image

```


